// Ad banner stub — renders nothing when AdMob is disabled.
// Preserves the AdBanner API so call sites (Home, Tools) keep working.
export function AdBanner(_props: { testID?: string }) {
  return null;
}
